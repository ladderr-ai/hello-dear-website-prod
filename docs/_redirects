# Netlify redirects for Single Page Application (SPA) routing
# This ensures that all routes are handled by the React app
# and not treated as 404s by the server

# Redirect all traffic to index.html for client-side routing
# except for files that exist (static assets)
/*    /index.html   200
